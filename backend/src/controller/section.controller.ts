import { RequestHandler } from "express";

export const getSections: RequestHandler = (req, res) => {
  res.json()
}

export const getSection: RequestHandler = (req, res) => {
  res.json()
}

export const updateSection: RequestHandler = (req, res) => {
  res.json()
}

export const createSections: RequestHandler = (req, res) => {
  res.json()
}

export const deleteSection: RequestHandler = (req, res) => {
  res.json()
}
